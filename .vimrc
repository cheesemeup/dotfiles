" No compatibility with vi should prevent unexpected issues (allegedly)
set nocompatible

" detect filetype, use appropriate colors
filetype plugin indent on
syntax on
colorscheme elflord

" The bell sound is too infuriating to retain
set visualbell

" Set tab width to 4 columns
set tabstop=4
" Line numbering
set number

" Visual warning for excessive line length
set colorcolumn=80

" Search settings
set ignorecase
set showmatch
set hlsearch

" Last line
set showmode
